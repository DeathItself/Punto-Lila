package com.example.pantallasapp.Fragments

import android.os.Bundle
import android.view.View
import androidx.core.app.NotificationCompat
import androidx.navigation.Navigation
import com.example.pantallasapp.Activity.MainActivity
import com.example.pantallasapp.R
import com.example.pantallasapp.databinding.ActivityRegistroBinding

class fragmentRegistro {
    private var _bin: ActivityRegistroBinding? = null
    private val bin get() = _bin!!

    /* fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

    }

    fun onCreate(savedInstanceState: Bundle?) {

        super.onCreate(savedInstanceState)


    }


    val textTitle = "Usuari Creat"
    val textContent = "L'usuari ha sigut creat correctament"
    var builder = NotificationCompat.Builder(this, MainActivity.CHANNEL_ID)
        .setSmallIcon(R.drawable.splash)
        .setContentTitle(textTitle)
        .setContentText(textContent)
        .setPriority(NotificationCompat.PRIORITY_DEFAULT)
*/

}